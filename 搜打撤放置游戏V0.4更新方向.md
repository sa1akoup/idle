# 搜打撤放置游戏 V0.4 更新方向

> 版本：V0.4  
> 状态：下一开发基线（方案，未开工）  
> 日期：2026-09-04  
> 前置：V0.3 A/B（合同购物清单、FIR 经济、v18 技能/声望）  
> 详细改造：`docs/v0.4-改造方案.md`

## 1. 产品判断

V0.3 已经能「为合同去带 FIR」。挂机本身不会缺基础资源：城区九宫格、商店补弹、出售变现都通。缺的是 **出发前多一个目的地**，以及 **垃圾和跨类战利品的销赃口**。

黑市现在 `Open: false`。LEDX、金链可以卖给黑市类目，螺丝只能卖机械，跨类货物卡在仓库。第二张图和 Boss 模板（基拉、施图尔曼）种子里有，城区节点和遭遇池都没挂，等于不会刷。

本版只做这两件事。不做 Scav 身份：挂机资源不靠白嫖局填。

## 2. 目标

- 黑市能收任意种类（含 FIR），价低于专精商人；也能卖一点杂物和低档弹药。
- 第二张图与城区战利品身份不同，合同链尾会把人带过去。
- 第二张图会刷 Boss（不是每局必打），打掉有精英背包和专属掉落。
- 上交仍手动。不接档案、不接 Scav 行动、不接通比特币。

## 3. 明确不做

- Scav 出发身份、Scav 冷却、Scav 箱生产
- 联络人档案、Kappa、Wipe、跳蚤、第三张图
- 酒精发生器/比特币农场/射击场买熟练度
- 动态全服物价、联合商人完整目录
- 局内操作

## 4. 关键取舍

| 选择 | 取 | 弃 |
|---|---|---|
| 商人 | 先开黑市 LL1（Fence） | 先做 Scav / 生产设施 |
| 地图 | 一张会刷 Boss 的第二图 | 城区加 Boss（冲掉现有节奏） |
| Boss | 低概率、指定节点、专属掉落 | 每局必打、全图随机 |
| 资源 | 靠现有挂机 + 黑市消化库存 | 再加一条白嫖收入 |

风味对照：黑市 ≈ Fence；基拉 ≈ 室内商场/厂房 Boss；施图尔曼 ≈ 林区远距 Boss。人名只存在现有模板 ID 里，对外文案继续用「【BOSS】铁腕·基拉 / 林驻·施图尔曼」。

## 5. 两块内容

### 5.1 黑市商人 LL1（先做）

打开 `black`。存量用户用数据适配解锁（同机械商人 v5，新版本号 v6）。

- **收购**：不限种类，价低于专精商人（约标价 20%～28%，随好感升高；专精约 30%～45%）。
- **货架**：每 6 小时刷新；从全部可售物品里按掉落权重抽约 24 格，越稀有/越贵越难出。
- **黑市卖给玩家**：标价翻倍后再走好感折扣。有库存，买完本轮消失。
- 出售涨的是黑市好感。合同入口仍在探索页，本版不加黑市合同链。

专精商人继续只收本类。

### 5.2 第二张图 + Boss

新图 `factory_woods`（林缘厂房，不是再做一座城区）：室内厂房适合基拉，室外林道/远距适合施图尔曼。节点少于城区（建议 6～7），遇敌更高，容器偏零件、工具、燃料、林野埋藏。

城区继续出夹克钥匙、医疗、金饰。工厂/林缘出零件、螺栓、电瓶、工具。武器/机械合同加探点和上交，购物清单会指向新图。

探索页地图下拉已经按 `/maps` 拉列表，`POST /session/start` 已带 `mapId`。第二张图主要是种子，不是新出发流程。

**Boss 怎么刷（对齐已有生成器，不新造一套）：**

城区现在不会刷 Boss，原因不是缺模板，而是没挂：

- `template_boss_killa` / `template_boss_shturman` 已在种子里；`enemygen` 已吃固定枪甲和名称。
- 城区节点全是 patrol/guard/elite；`encounterPools()` 没有 `Role=boss`。
- 快照遭遇池按 `map_id` 过滤，新图的 boss 池进不了城区局。

新图做法：

- 城区遭遇池保持现状，**不挂** boss 模板。
- 室内高价值节点默认敌人 = 基拉，室外远距节点默认敌人 = 施图尔曼；`EncounterRole=boss`，`EncounterChance` 约 25～40，不是 100。
- 新图仍要有 `Role=boss` 遭遇池（快照校验要求角色池非空；事件若覆盖遭遇也走池）。
- 击倒：精英背包照旧；本版把模板上已有的 `BossLootItems` 接到击杀掉落（GPU/LEDX/高档零件等少量高价值，仍 FIR）。
- 合同可加「击倒 1 名 Boss」：`defeat_kind` + `kind=boss`，现有进度逻辑已按 Kind 计数。

进行中的城区 Session 冻在旧快照，不受新图种子影响。

## 6. 实施顺序

```
A. 黑市 LL1     开放、任意种类收购、低价目录、存量解锁
B. 第二张图     拓扑/容器/事件、选图出发、合同延伸
C. Boss         新图遭遇池 + 指定节点 + BossLootItems 接入
```

A 可独立上线。B 的图没有 Boss 也能玩；C 是这张图的风味，不单独开第三阶段文档，跟 B 同一张图交付。

## 7. 架构约束

- 黑市只改 service 出售/目录过滤和 Seed，不升引擎版本。
- 新图用现有节点/容器/事件/生成器。Boss 用已有 `Kind=boss` 模板，不改战斗公式。
- 击杀掉落只在 `Kind=boss` 且 `BossLootItems` 非空时多滚 1 件；旧城区局没有 Boss，可仍 v18。
- 合同上交保持手动。不要给每个玩家预写新图进度。
- 生产发布：`migrate` 后必须 `seed`（新图、遭遇池、黑市 Open）。
