# 搜打撤放置游戏在线方向改造方案

> 版本：V1.0  
> 状态：已实施架构基线
> 更新日期：2026-08-25
> 适用项目：`D:/idle`  
> 目标：将当前单机单存档原型改造成支持用户登录、在线查看、离线挂机、局外养成和服务器部署的多用户在线游戏。

## 1. 改造目标

### 1.1 产品目标

完成改造后，系统应支持：

1. 用户注册、登录、退出和会话续期。
2. 每个用户拥有独立角色、仓库、装备、商人关系和藏身处状态。
3. 玩家在线时可以实时查看探索进度和关键行动事件。
4. 玩家离线后探索继续推进，重新上线时可以补算并查看离线结果。
5. 服务重启、升级或短暂故障后，行动可以继续推进，不重复结算。
6. 购买、出售、维修、掉落、行动结算和丢装结算具备事务一致性。
7. 后续可以通过增加 Worker 实例扩展处理能力。

### 1.2 第一阶段边界

第一阶段继续保持以下约束：

- 单用户只拥有一个可出战角色。
- 单角色同一时间最多进行一个探索会话。
- 不做玩家交易、拍卖行、公会和多人战斗。
- 不做微服务拆分。
- PostgreSQL 是生产环境唯一权威数据源。
- Redis 仅在出现明确性能需求后引入，不保存权威游戏状态。
- SQLite 保留给本地快速开发和纯引擎单元测试。

### 1.3 当前实现状态

当前项目已完成本方案的核心在线化改造：

- 用户注册/登录、HttpOnly Cookie、Bearer Token 与用户数据隔离。
- `internal/engine` 纯引擎、不可变 `ScenarioSnapshot` 和版本化确定性重放。
- 数据库到期任务、lease、Pending Run 完整性哈希、事务结算和崩溃恢复。
- 真实时间驱动的离线补算与 `waiting_injury` 伤势等待。
- `session_events` 持久化事件、REST 游标查询、SSE 实时推送和历史回放。
- 多口径 N1～N6 弹药、仓库堆叠、商人等级限制、自动降级补给。
- 敌人护甲和弹药、护甲覆盖/无甲区域、穿甲等级、肉伤与护甲耐久计算。
- 前端实时探索页、战斗事件展示、断线重连和终态补拉。

仍需后续扩展的内容：

- PostgreSQL 多实例 Worker 的容量压测与生产观测指标。
- 藏身处设施、制造和长期队列。
- 更完整的分页、审计和后台管理能力。
- 实时界面的容器、敌人和战斗专属视觉资源。

## 2. 核心设计原则

### 2.1 在线和离线使用同一纯引擎

在线探索与离线补算都执行相同的 `SimulateRun(ScenarioSnapshot, RunInput)`。在线状态只影响事件何时展示，不改变路线、战斗、掉落、消耗、伤势和终态。关闭页面后数据库调度器继续处理已经到期的局，用户回来后读取同一批持久化事件。

### 2.2 服务端权威

客户端只提交玩家意图，例如地图、装备和中止操作。以下内容必须由服务端计算：

- 服务器时间。
- 随机 seed 和 RNG 游标。
- 路线、事件和战斗结果。
- 掉落、价格、结算和物品消耗。
- 伤势恢复、设施完成和冷却结束时间。

### 2.3 用户数据强隔离

所有运行态数据必须能追溯到 `user_id`。任何包含资源读取或修改的查询，都必须同时检查资源 ID 和当前登录用户。

### 2.4 数据库保存事实

数据库保存角色当前状态、行动游标和已发生事件。SSE、缓存和内存状态只负责加速展示，不能成为游戏状态的唯一来源。正式多实例部署使用 PostgreSQL；2c4g 单机档位允许使用 SQLite WAL，前提是采用单进程、短事务和单写入调度。

### 2.5 结算可重试

行动推进、购买、出售、维修和奖励领取必须支持安全重试。相同操作重复提交时，只允许产生一次状态变化。

### 2.6 分阶段替换

每一步先建立新能力，再迁移调用方，最后删除旧路径。数据库结构变更优先采用可向前兼容的新增字段和新增表，避免一次性重写全部模块。

## 3. 目标架构

```text
Browser
  |
  | HTTPS / SSE
  v
Caddy or Nginx
  |-- Vue static assets
  `-- /api/* -> Go API
                   |
                   |-- Authentication middleware
                   |-- Game command/query services
                   |-- Realtime event stream
                   |
                   v
              PostgreSQL
                   ^
                   |
              Session Worker
              Facility Worker
```

常规生产架构可以由一个 Go 进程同时运行 API 和 Worker。2c4g 单机档位不部署独立消息队列、Redis 或多个 Worker，先使用一个 API 进程、一个低频调度循环和一个轻量结算执行器。代码层仍保持 API、调度和结算生命周期独立，后续扩容时才能拆分。

当前目录边界：

```text
backend/
├── internal/
│   ├── engine/               # 纯模拟、快照校验、事件轨迹和重放
│   ├── config/               # 静态内容与运行配置
│   ├── models/               # 持久化模型
│   ├── repository/database/  # 数据库连接与版本化 migration
│   ├── service/              # 调度、资源准备、持久化和事务结算
│   └── handler/              # 认证 API、业务 API 与 SSE
└── migrations/
```

API 与 Worker 当前由同一个 Go 进程承载，调度和结算逻辑保持独立，后续可按负载拆为独立进程。

## 4. 目标数据模型

### 4.1 账号与登录会话

| 表 | 关键字段 | 说明 |
|---|---|---|
| `users` | `id, username, email, password_hash, status, created_at` | 用户主表 |
| `auth_sessions` | `id, user_id, token_hash, expires_at, last_seen_at, revoked_at` | 服务端登录会话 |

约束：

- `username` 和规范化后的 `email` 唯一。
- 数据库只保存 Session Token 的哈希值。
- 用户禁用后立即拒绝新请求，并撤销现有登录会话。

### 4.2 玩家运行态

| 表 | 用户归属 | 改造方式 |
|---|---|---|
| `characters` | `user_id` | 每个用户第一阶段只创建一个角色 |
| `player_loadouts` | 通过 `character_id` | 删除固定主键假设 |
| `inventories` | `user_id` | 唯一索引包含 `user_id` |
| `armor_instances` | `user_id` | 防止读取或维修他人实例 |
| `user_merchant_states` | `user_id + merchant_id` | 将商人定义与玩家好感度拆开 |
| `hideout_facilities` | `user_id + facility_id` | 保存设施等级和运行状态 |
| `facility_jobs` | `user_id` | 保存制造、治疗和升级队列 |

静态内容继续使用共享定义表：

- 武器、护甲、补给和战利品定义。
- 地图、节点、容器、敌人和事件定义。
- 商人和藏身处设施定义。

### 4.3 探索会话

当前 `sessions` 已作为持久化行动实体：

| 字段 | 作用 |
|---|---|
| `id` | 数字主键，接口按用户归属访问 |
| `user_id` | 行动所有者 |
| `character_id` | 出战角色 |
| `status` | `running/waiting_injury/finished/aborted/failed` |
| `seed` | 随机种子 |
| `engine_version` | 模拟规则版本 |
| `scenario_snapshot` | 完整不可变场景快照 JSON |
| `scenario_hash` | 规范化快照哈希 |
| `initial_state_json/state_json` | 初始状态与已结算跨局状态 |
| `pending_run_index/result/hash` | 尚未结算的完整 RunPlan 与完整性校验 |
| `current_run_started_at` | 当前局现实时间轴起点 |
| `next_run_at` | 当前局预计结算或伤势恢复时间 |
| `last_processed_at` | 上一局完成结算时间 |
| `lease_owner/lease_until/heartbeat_at` | Worker 原子领取与租约信息 |
| `offline_limit_sec/elapsed_sec` | 会话时间窗与已结算秒数 |

行动启动时必须保存完整快照：

- 角色属性和技能。
- 武器、护甲、胸挂、背包、头盔、耳机和补给。
- 护甲实例 ID 与当前耐久。
- 地图、行动策略和恢复预设。
- 引擎版本、快照 schema 版本和完整静态内容。

### 4.4 结构化事件

当前使用 `session_events`：

```text
id
user_id
session_id
run_index
sequence
event_type
offset_sec
available_at
node_id
subject_id
payload_json
created_at
```

建议事件类型：

```text
run_started
node_entered
container_search_started
loot_found
loot_collected
container_search_finished
battle_started
battle_attack
battle_round
battle_escape
battle_finished
evacuation_started
run_settled
loot_extracted
loot_stored
loot_overflow
ammo_refilled
session_finished
session_aborted
session_failed
```

事件使用 `(session_id, run_index, sequence)` 唯一约束。引擎只生成相对 `offset_sec`，Service 根据本局开始时间生成 `available_at`；REST 和 SSE 只返回已到展示时间的事件。

前端行动报告由事件渲染生成。资源扣除直接读取事件结果对象，禁止通过报告文本匹配关键词。

### 4.5 经济流水

新增 `economy_ledger`：

```text
id
user_id
operation_id
operation_type
currency_delta
item_id
item_delta
reference_type
reference_id
created_at
```

`operation_id` 必须唯一，用于保证购买、出售、行动结算和奖励领取不会重复执行。

## 5. 详细实施顺序

## 5.1 第 0 步：冻结当前基线

### 目标

建立可重复验证的单机基线，防止服务化改造期间无法判断玩法是否发生回归。

### 后端工作项

1. 为当前数据库建立只读备份，不直接使用唯一的 `idle.db` 做迁移实验。
2. 固定一组角色、装备、地图和 seed 组合。
3. 将 `simulateSingleRun` 的关键结果固化为测试样例。
4. 补充以下测试：
   - 起点到撤离点的路线合法。
   - 容器搜刮受格数和重量限制。
   - 同 seed、同输入得到相同输出。
   - 弹药、护甲和补给不会出现负数。
   - 单局结算只执行一次。
5. 使用固定 seed 批量模拟记录撤离率、伤势率、收益和消耗分布，作为内部回归基线。

### 前端工作项

1. 保存探索、角色、仓库和日志页面的关键截图。
2. 记录当前 API 响应格式，作为后续 DTO 迁移对照。

### 验收条件

- `go test ./...` 稳定通过。
- 相同 seed 可以稳定复现。
- 已记录基线分布和页面状态。

### 回滚方式

本步骤不修改生产数据结构，只需要恢复代码和数据库备份。

## 5.2 第 1 步：建立配置和数据库迁移体系

### 目标

让项目可以在本地 SQLite 和生产 PostgreSQL 之间切换，并停止依赖生产启动时自动建表。

### 后端工作项

1. 增加配置结构：

```text
APP_ENV
HTTP_ADDR
DATABASE_DRIVER
DATABASE_URL
SESSION_SECRET
TRUSTED_PROXIES
ALLOWED_ORIGINS
```

2. 将数据库初始化从 `main.go` 移到独立 `repository/database` 包。
3. 支持 `sqlite` 和 `postgres` 两种驱动。
4. 建立版本化 SQL migration 目录。
5. 本地开发可以保留显式 migration 命令，生产启动禁止直接调用 `AutoMigrate`。
6. seed 分为两类：
   - 静态内容 seed，可以幂等更新。
   - 玩家初始数据，只在注册事务中创建一次。
7. 增加数据库连接池、连接超时和事务超时配置。

### 数据迁移

第一份 migration 只复刻当前结构，不立即增加用户字段。先验证 PostgreSQL 与现有查询行为一致。

### 验收条件

- 同一套集成测试可以分别运行在 SQLite 和 PostgreSQL。
- 空 PostgreSQL 数据库可以通过 migration 和静态 seed 启动。
- 重复执行 migration 不会破坏已有数据。
- 生产模式缺少必要配置时拒绝启动。

### 回滚方式

部署前保留旧 SQLite 服务。PostgreSQL 验证失败时切回旧版本和旧数据库，禁止双向写入。

## 5.3 第 2 步：引入用户归属和数据隔离

### 目标

先建立多用户数据边界，再接入登录入口。

### 数据库工作项

1. 创建 `users` 表。
2. 给 `characters`、`inventories`、`armor_instances`、`sessions` 增加 `user_id`。
3. 将商人表拆分：
   - `merchant_defs` 保存共享定义。
   - `user_merchant_states` 保存好感度、解锁和个人状态。
4. 给运行态表增加外键和组合索引。
5. 库存唯一索引调整为：

```sql
UNIQUE (user_id, item_id, raid_extract)
```

6. 限制单角色进行中行动：

```sql
CREATE UNIQUE INDEX uq_active_expedition_character
ON expeditions(character_id)
WHERE status IN ('scheduled', 'running', 'waiting', 'aborting');
```

### 数据迁移工作项

1. 创建一个迁移用默认用户。
2. 将现有角色、仓库、装备实例、会话和商人好感度归属到默认用户。
3. 所有 `user_id` 回填完成后再增加 `NOT NULL`。
4. 验证数量、现金、装备耐久和历史行动前后完全一致。

### 后端工作项

1. 删除 `PlayerCharacterID` 和 `PlayerLoadoutID` 业务依赖。
2. Service 方法显式接收 `userID` 或 `characterID`。
3. Repository 查询统一包含用户作用域。
4. `ListSessions`、`GetSession`、维修、购买、出售全部增加归属校验。
5. 全局“存在进行中行动”改成当前角色范围判断。

### 测试要求

创建用户 A 和用户 B，覆盖：

- A 无法读取、修改、中止或结算 B 的行动。
- A 无法维修或出售 B 的装备。
- 两个用户可以同时开始行动。
- 相同物品可以分别存在于两个用户仓库中。

### 验收条件

- 所有运行态表都有明确用户归属。
- 越权测试全部返回 `404` 或统一的无权限响应。
- 业务代码中不再引用固定玩家主键。

### 回滚方式

本阶段采用新增字段和新表。旧字段暂时保留一版，确认新查询稳定后再删除。

## 5.4 第 3 步：实现登录认证和授权中间件

### 目标

建立安全的网页登录会话，并让全部玩家 API 从登录上下文获得 `user_id`。

### 后端工作项

1. 实现接口：

```text
POST /api/auth/register
POST /api/auth/login
POST /api/auth/logout
GET  /api/auth/me
```

2. 当前密码使用 bcrypt；Argon2id、注册/登录限流和失败退避列为上线前安全加固项。
3. 生成高熵随机 Session Token，数据库保存 `token_hash`。
4. Cookie 建议：

```text
__Host-session=<token>
Secure
HttpOnly
SameSite=Lax
Path=/
```

5. 登录成功后轮换 Session ID。
6. 登录 Session 保存过期时间、最后活跃时间、撤销时间。
7. 增加 `AuthMiddleware`，把 `user_id` 写入 Gin Context。
8. 所有玩家私有接口移入认证路由组。
9. 写接口增加 CSRF Token 或自定义 Header 校验，并校验 `Origin`。
10. 增加登录失败退避、账号禁用和 Session 撤销能力。

### 前端工作项

1. 引入 Vue Router，至少包含：

```text
/login
/register
/game
```

2. Axios 开启同源 Cookie，并统一处理 `401`。
3. 未登录时进入登录页，登录成功后请求 `/auth/me`。
4. 退出登录后清空内存中的玩家状态。
5. 禁止把 Session Token 保存到 `localStorage`。

### 验收条件

- 未登录请求玩家资源返回 `401`。
- 登录后只能访问当前用户数据。
- Cookie 无法被前端 JavaScript 读取。
- 退出登录后旧 Session 立即失效。
- 修改密码或禁用账号后可以撤销全部 Session。

### 回滚方式

保留只在测试环境启用的临时默认用户登录入口。生产环境禁止保留绕过认证的兼容路径。

## 5.5 第 4 步：稳定 API 合同和前端启动流程

### 目标

减少首屏请求数量，建立适合长期演进的 API 版本和错误规范。

### 后端工作项

1. 当前接口统一使用 `/api`；引入 `/api/v1` 前先完成 DTO 和迁移窗口设计。
2. 统一错误格式：

```json
{
  "code": "EXPEDITION_ALREADY_RUNNING",
  "message": "当前角色已有行动正在进行",
  "requestId": "..."
}
```

3. 后续可增加 `/api/v1/bootstrap`，返回：
   - 当前用户和角色摘要。
   - 当前装备和仓库容量。
   - 当前行动摘要。
   - 静态内容版本。
4. 静态内容提供独立版本化接口，支持 `ETag` 或版本号缓存。
5. 历史行动和事件接口增加游标分页。
6. 为购买、出售、维修、开始行动等写接口接受 `Idempotency-Key`。
7. API DTO 与 GORM Model 分离，避免数据库字段直接成为长期接口合同。

### 前端工作项

1. 将当前首屏多个并发请求替换为 `bootstrap + static-content`。
2. 静态内容按 `contentVersion` 缓存。
3. 对 `401`、`403/404`、`409`、`429` 建立明确交互。
4. 当前行动状态独立管理，避免刷新仓库时覆盖实时行动数据。

### 验收条件

- 登录后的核心首屏请求减少到 2 至 4 个。
- 重复提交写请求不会产生重复状态变化。
- 历史行动数量增加后不会一次性返回全部记录。

### 回滚方式

引入 `/api/v1` 时保留 `/api` 一个迁移版本，前端完成切换后再删除旧路由。

## 5.6 第 5 步：拆出确定性模拟引擎

### 目标

让模拟逻辑脱离数据库、HTTP 和后台 goroutine，成为可以重放和批量验证的纯逻辑模块。

### 引擎接口

当前核心接口：

```go
type RunInput struct {
    SessionSeed int64
    RunIndex    int
    Style       string
    State       EngineState
}

type RunResult struct {
    Result        string
    DurationSec   int64
    AmmoUsed      int
    Injury        string
    ExtractedLoot []LootDrop
    ConsumedItems []ItemStack
    Trace         []TraceEvent
    NextState     EngineState
}

func SimulateRun(snapshot ScenarioSnapshot, input RunInput) (RunResult, error)
```

### 后端工作项

1. 地图寻路、容器搜刮、事件、战斗和撤离位于 `internal/engine`。
2. 引擎输入只能使用完整快照，禁止在模拟中临时查询当前装备或当前商人状态。
3. 引擎输出结构化 `TraceEvent`、资源消耗和 `NextState`。
4. RNG 由 seed 与 sequence 驱动，禁止直接使用当前时间生成中间随机数。
5. 保存 `engine_version`、完整 `scenario_snapshot` 和 `scenario_hash`。
6. 老行动继续使用启动时版本，数值更新只影响新行动。
7. 报告渲染器根据结构化事件生成中文文本。
8. 删除通过 `strings.Contains(report, ...)` 判断资源消耗的路径。

### 测试要求

- 同输入、同版本、同 seed、同 sequence 输出完全相同。
- 引擎测试不连接数据库。
- 10000 次模拟中不存在非法路线、负资源或超容量携带。
- 每种装备和关键属性至少有一条效果测试。
- Trace 序号连续、时间偏移不为负且单调不倒退。

### 验收条件

- 引擎可以在命令行测试中独立运行。
- seed 重放能够还原完整事件序列。
- Service 只负责读取快照、调用引擎和事务结算。

### 回滚方式

保留旧模拟入口作为测试对照，禁止新旧引擎同时写正式玩家数据。

## 5.7 第 6 步：实现持久化行动调度和离线补算

### 目标

使用数据库到期任务替代会话专属 goroutine，让行动跨服务重启持续推进，并保证在线与离线执行同一纯引擎结果。

### 时间语义

当前时间规则：

1. 引擎使用秒级时间轴，配置中的 1 分钟按 60 秒换算。
2. `current_run_started_at` 是本局时间轴起点，`next_run_at` 是本局结算或伤势恢复到期时间。
3. Worker 只处理 `next_run_at <= now` 的 Session，服务器停机时间自然计入补算。
4. 每局结算后将 `last_processed_at` 推进到本局完成时间，再基于下一局持续时间设置新的 `next_run_at`。
5. 伤势使用 `waiting_injury`，此时 `next_run_at = injury_until`，到期后再准备资源并规划下一局。
6. 单次 Worker 调用最多补算 100 局，超过后留给下一轮调度，避免长期占用执行槽。

### 调度方式

调度器查询 `running/waiting_injury` 且 `next_run_at <= now` 的 Session，通过 `lease_owner + lease_until` 条件更新原子领取任务。Worker 持续更新 `heartbeat_at`，租约过期后其他 Worker 可以重新领取。SQLite 单机使用一个进程；PostgreSQL 可按压测结果增加 Worker。

### 单次推进事务

```text
BEGIN
  校验 Session 状态、租约和用户资源版本
  解码并校验 Pending Run 的 run_index + input state + result hash
  写入唯一的 session_run
  结算掉落、弹药、消耗品、护甲、角色和商人关系
  更新 state_json、last_processed_at 与 elapsed_sec
  若继续行动，准备资源并用纯引擎规划下一局
  保存下一局 Pending Run、事件和 next_run_at
  若结束，写入统一终态事件和结束原因
COMMIT
```

### 关键约束

- `(session_id, run_index)` 保证 Worker 崩溃重试时不重复结算单局。
- `(session_id, run_index, sequence)` 保证事件不会重复写入。
- Pending Run 在计划时已经完成模拟，恢复时先校验再原样结算，禁止重新模拟。
- 用户资源通过 `characters.resource_version` 和带数量条件的库存更新保护，防止并发负数与覆盖。
- 伤势恢复和资源准备在事务内形成明确终态；补给失败写入 `reason=resource_unavailable`。
- Worker 关闭时停止领取新作业，允许当前事务完成。
- 启动时自动扫描到期行动，不依赖内存作业列表。
- 单个行动失败只标记该行动，不影响 Worker 主循环。

### 在线与离线行为

- 玩家在线：SSE 按 `available_at` 播放已经持久化的细粒度事件，REST 负责首屏和断线补拉。
- 玩家离线：数据库调度器继续结算到期局，不依赖浏览器连接或登录请求。
- 玩家重新上线：读取已发生事件和当前 Session；未到展示时间的事件继续由 SSE 推送。
- 在线状态不会改变模拟粒度、RNG 消耗、资源消耗或最终结果。
- 离线窗口截止、资源不足、失能补购失败、护甲损坏或玩家中止时停止规划后续局。

### 容量模型与并发边界

每个离线角色只对应数据库中的 Session、RunPlan 和事件数据，不创建会话专属 goroutine。CPU 负载集中在规划下一局和到期事务结算。

当前原型的负载放大方式如下：

```text
单次离线补算局数 = min(已到期局数, 100)
总模拟量 = 到期 Session 数 × 实际规划局数 × 每局节点/战斗事件数
```

单机建议配置：

| 项目 | 建议值 |
|---|---|
| Go 进程 | 1 个 API + 调度器进程 |
| 调度并发 | 当前 Worker Pool 为 4、每批领取 32、队列容量 64；按数据库和 CPU 压测调整 |
| 数据库 | 本地/低并发单实例使用 SQLite，正式多实例使用 PostgreSQL |
| SQLite | 单 Backend 进程，连接启用 `busy_timeout=5000`，经济变化保持短事务 |
| 实时查看 | SSE 主通道 + REST 游标补拉，服务端定期 heartbeat |
| 日志 | 持久化结构化局内事件、结算事件和文本报告 |
| 单次补算 | 每次 Worker 最多 100 局，后续轮次继续领取 |
| 同时在线 | 以 SSE 连接、事件写入和补拉查询压测结果确定上限 |

压测至少覆盖大量 Session 同时到期、100 局补算上限、SSE 长连接、断线补拉、SQLite 写冲突和服务重启恢复。若数据库持续锁等待或事件查询延迟升高，先限制 Worker 并发和单批领取数，再扩展 PostgreSQL 与独立 Worker。

### 验收条件

- 行动过程中重启 API 和 Worker，恢复后继续生成正确事件。
- 两个 Worker 同时运行不会重复结算。
- 在线和离线在相同快照、seed、run index 与状态下得到相同事件和终态。
- Pending Run 恢复后原样结算，不重新模拟、不重复扣除资源。
- 中止和结算并发时只出现一个合法终态。

### 回滚方式

回滚前停止创建新行动并停止 Worker 领取任务，等待当前事务完成。代码回滚必须与支持当前 `engine_version`、快照 schema 和 migration 的数据库版本配套。

## 5.8 第 7 步：实现实时行动查看

### 目标

让玩家在线时看到关键行动节点，并在断线重连后继续追赶。

### 历史与断线补拉

接口：

```text
GET /api/session/:id/events?afterId=128
```

响应包含：

```json
[
  {
    "id": 142,
    "sessionId": 7,
    "runIndex": 2,
    "sequence": 8,
    "eventType": "loot_found",
    "offsetSec": 95,
    "payload": {}
  }
]
```

接口直接返回事件数组。前端按事件 `id` 合并 REST 与 SSE 数据，并从数组中维护最大事件 ID 作为 cursor。REST 返回后执行 merge，不覆盖 SSE 已经收到的事件。

### SSE 实时推送

接口：

```text
GET /api/session/:id/events/stream?afterId=142
Last-Event-ID: 142
```

SSE 只推送已经持久化且 `available_at <= now` 的事件，支持 query cursor 和 `Last-Event-ID`。连接期间发送 heartbeat；Session 进入终态后发送 `stream_end`。断线后客户端先用最后事件 ID 补拉，再恢复流。

### 前端状态机

```text
connecting
live
reconnecting
catching_up
finished
failed
```

客户端使用 `1s、2s、4s、8s…` 指数退避，最大等待 30 秒。连接成功后重置计数；组件卸载时设置 stopped 状态并清理 `EventSource` 与 timer，禁止残留重连。

### 展示粒度

推荐实时推送：

- 进入节点。
- 搜索容器、发现物资和采集完成。
- 遭遇、每轮攻击、命中区域、弹药/护甲等级、伤害和脱离结果。
- 开始撤离和最终结算。
- 弹药自动补给、仓库入库、溢出与 Session 终态。

战斗仍为服务端自动决策，实时界面负责播放，不接收玩家逐回合操作。容器和敌人图片按 `subject_id` 映射本地素材，缺少专属素材时使用统一占位图。

### 验收条件

- 页面刷新后通过 REST 恢复历史，再从最大事件 ID 继续 SSE，无丢失或重复日志。
- SSE 断开后能够自动补拉。
- 用户只能订阅自己的行动。
- 慢客户端不会阻塞 Worker 和其他用户。
- 已完成、已中止和失败 Session 可从历史页进入事件详情，不建立持续 SSE。

### 回滚方式

SSE 出现问题时关闭实时流入口，前端回退到游标轮询，数据库事件模型不需要回滚。

## 5.9 第 8 步：强化经济、库存和幂等结算

### 目标

让所有局内外资源变化具备审计、并发安全和防重复能力。

### 后端工作项

1. 购买、出售、维修、补购、行动结算全部生成 `operation_id`。
2. 钱包与库存更新放在同一事务中。
3. 统一资源锁定顺序：

```text
user wallet
inventory stack
equipment instance
expedition
```

所有业务保持同一顺序，降低死锁概率。

4. 数据库增加约束：
   - 现金余额不得为负。
   - 库存数量不得为负。
   - 护甲当前耐久不能超过最大耐久。
5. 商人好感度、开放状态和购买解锁改为用户维度。
6. 当前装备和行动快照中的装备禁止被出售或重复使用。
7. 仓库容量检查和物品写入必须处于同一事务。
8. 记录所有管理补偿和异常修复流水。

### 测试要求

- 同一购买请求并发提交十次只扣一次钱。
- 行动结算超时重试不会重复发放战利品。
- 中止、丢装、补购和维修并发时库存保持合法。
- 用户无法出售行动中已锁定装备。

### 验收条件

- 任意资源变化都可以通过 `operation_id` 追溯来源。
- 数据库中不存在负现金、负库存和无归属装备。

### 回滚方式

流水表只追加不删除。业务回滚通过生成反向补偿操作完成，不直接修改历史流水。

## 5.10 第 9 步：实现局外养成和持久队列

### 目标

让藏身处、治疗、训练和制造使用与探索相同的持久化时间模型。

### 设施模型

共享定义：

```text
facility_defs
facility_level_defs
recipe_defs
```

玩家状态：

```text
hideout_facilities
facility_jobs
```

### 实施顺序

1. 医疗床：缩短或管理伤势恢复。
2. 维修台：维修作业进入队列，完成后修改实例耐久。
3. 储物间：设施等级决定仓库容量。
4. 工坊：消耗物资制造补给。
5. 情报室：提升预估信息质量和地图情报。
6. 训练场：消耗时间和资源获得定向熟练经验。

### 调度方式

`facility_jobs.complete_at` 使用与行动 Worker 相同的到期领取方式，不为每个作业创建独立 goroutine 或定时器。

### 验收条件

- 玩家离线时设施作业按服务器时间完成。
- 服务重启不会丢失制造、治疗或升级作业。
- 设施消耗和产出有完整流水。

### 回滚方式

设施能力按功能开关逐项启用。发生问题时暂停领取新作业，保留已创建作业并通过补偿事务处理。

## 5.11 第 10 步：生产部署基础设施

### 目标

建立可重复部署、可观察、可备份和可回滚的生产环境。

### 部署单元

建议准备：

```text
frontend build artifact
Go application image
PostgreSQL
Caddy or Nginx
backup job
```

初期部署可以使用 Docker Compose。2c4g 单机版的实际部署单元收敛为：

```text
Caddy/Nginx + Go API/调度器 + SQLite WAL + 备份作业
```

2c4g 档位暂不部署 Redis、Kafka、独立 Worker 和独立 PostgreSQL 容器，避免内存、连接数和运维开销被基础设施消耗。用户量和可用性要求提高后，再迁移到托管数据库并拆分 Worker。

2c4g 运行约束：

- Go 进程只启动一个实例，使用优雅关闭和单实例锁，避免 SQLite 多进程写入。
- SQLite 开启 WAL、busy timeout 和定期 checkpoint；数据库文件放在持久化磁盘。
- 数据库连接池限制为 4 个连接，所有库存、奖励和行动结算使用短事务。
- 日志按大小轮转，禁止无限保存逐节点报告。
- 每日备份 SQLite 文件，备份完成后执行一次恢复演练。

### 网络和 TLS

1. 浏览器只访问 HTTPS。
2. Vue 与 API 使用同一主域，减少 CORS 和 Cookie 复杂度。
3. 反向代理负责 TLS、请求体限制和基础安全 Header。
4. Go 配置真实可信代理范围，禁止无条件信任外部转发头。
5. 生产 CORS 只允许明确域名，凭证模式禁止通配来源。

### 启动流程

```text
检查配置
等待数据库可用
执行独立 migration job
启动 API
启动 Worker
执行 readiness 检查
切换流量
```

### 健康检查

```text
GET /health/live
GET /health/ready
```

- `live` 只证明进程存活。
- `ready` 检查数据库连接和必要静态内容版本。
- Worker 额外暴露最后成功领取和结算时间。

### 优雅关闭

1. 收到关闭信号后停止接受新请求。
2. Worker 停止领取新作业。
3. 等待当前事务完成或达到超时。
4. 关闭 SSE 连接，并让客户端进入重连状态。
5. 关闭数据库连接。

### 验收条件

- 新环境可以从空数据库自动部署到可登录状态。
- API 滚动重启期间已开始行动不会丢失。
- migration 失败时不会启动新版本 API。
- 可以在规定时间内回退应用镜像。

## 5.12 第 11 步：监控、备份和安全加固

### 结构化日志

每条关键日志包含：

```text
request_id
user_id
expedition_id
operation_id
worker_id
duration_ms
error_code
```

禁止记录：

- 明文密码。
- 完整 Session Token。
- CSRF Token。
- 数据库连接密码。

### 指标

至少监控：

- HTTP 请求量、延迟和错误率。
- 注册、登录成功和失败次数。
- 当前在线用户和 SSE 连接数。
- 进行中行动数量。
- 到期行动积压数和最大延迟。
- Worker 成功、失败和重试次数。
- 行动结算耗时。
- 数据库连接池、慢查询和锁等待。
- 经济操作失败和幂等冲突数量。

### 备份

1. 定期 PostgreSQL 备份。
2. 备份文件异地或跨磁盘保存。
3. 定期执行恢复演练。
4. 恢复后校验用户数、现金总量、库存数量、行动事件连续性。
5. 版本升级前执行额外备份。

### 安全

- 登录、注册和 Session 启动接口限流。
- 输入长度、枚举值和数量范围统一校验。
- 设置 CSP、`X-Content-Type-Options` 和合理的 Referrer Policy。
- 管理后台使用独立权限模型，不允许通过普通用户 Session 访问。
- 定期更新依赖并运行漏洞扫描。
- 提供用户 Session 列表和主动撤销入口。

### 验收条件

- 可以从日志追踪一次行动的完整生命周期。
- Worker 积压和结算异常能够触发告警。
- 已完成至少一次从备份恢复到测试环境的演练。

## 5.13 第 12 步：测试、灰度和正式发布

### 测试层级

#### 单元测试

- 密码哈希和 Session 校验。
- 地图寻路、事件、战斗和搜刮。
- 时间推进和离线补算。
- 报告事件渲染。

#### 集成测试

- PostgreSQL migration。
- 用户隔离和权限校验。
- 行动领取、行锁、重试和幂等。
- 钱包、库存和装备事务。

#### 端到端测试

```text
注册
登录
配置装备
开始行动
在线查看事件
关闭页面
等待离线推进
重新登录
补拉事件
成功撤离
检查库存和行动结算
```

#### 故障测试

- Worker 在结算事务前后崩溃。
- API 在开始行动请求返回前崩溃。
- SSE 频繁断开。
- 数据库短暂不可用。
- migration 中途失败。

#### 压力测试

逐步验证：

- 100、500、1000 个同时进行的行动。
- 多 Worker 同时领取到期作业。
- 大量用户同时上线补拉离线事件。
- 行动历史达到数万条后的分页性能。

### 灰度发布

1. 内部测试用户。
2. 小规模邀请用户。
3. 只开放注册，不开放付费和玩家交易。
4. 观察至少一个完整挂机周期。
5. 确认经济、积压、错误率和恢复流程稳定后扩大用户量。

### 正式发布门槛

- 两个用户之间不存在数据越权。
- 相同 seed 可以重放。
- 在线与离线使用相同纯引擎，在相同输入下产生相同资源、奖励、事件和终态。
- 服务重启不会丢失或重复结算。
- 重复购买、出售和奖励请求只生效一次。
- Worker 积压有监控和恢复方案。
- PostgreSQL 备份已经验证可恢复。
- 生产 migration 和应用镜像都可以回退。

## 6. 推荐迭代拆分

| 迭代 | 范围 | 可交付结果 | 前置依赖 |
|---|---|---|---|
| R1 | 基线测试、配置、PostgreSQL、migration | 可在 PostgreSQL 运行的单用户版本 | 无 |
| R2 | `user_id`、数据迁移、用户隔离 | 多个测试用户拥有独立存档 | R1 |
| R3 | 注册登录、Session Cookie、API v1 | 可登录的多用户版本 | R2 |
| R4 | 纯模拟引擎、结构化事件、seed 重放 | 可重放的行动结算 | R1 |
| R5 | 数据库调度、lease、RunPlan 和真实时间补算 | 服务重启后继续行动，重试不重复结算 | R3、R4 |
| R6 | 持久化事件、REST 游标、SSE 和前端实时状态 | 在线实时查看、断线追赶与历史回放 | R5 |
| R7 | 经济流水、幂等和装备锁定 | 可审计且防重复的经济系统 | R5 |
| R8 | 藏身处队列、治疗和制造 | 完整局外养成闭环 | R7 |
| R9 | 容器部署、监控、备份、灰度 | 可公开部署的生产版本 | R3-R8 |

R2 和 R4 可以在 R1 完成后并行开发，但合并时必须以 R2 的用户隔离模型作为最终数据库结构。

## 7. 当前 API 清单

### 7.1 认证

```text
POST /api/auth/register
POST /api/auth/login
POST /api/auth/logout
GET  /api/auth/me
```

### 7.2 玩家与静态内容

```text
GET /api/player
PUT /api/player
GET /api/weapons
GET /api/ammos
GET /api/armors
GET /api/armor-instances
GET /api/consumables
GET /api/loot
GET /api/chestrigs
GET /api/backpacks
GET /api/helmets
GET /api/headsets
GET /api/maps
GET /api/nodes
GET /api/enemies
```

### 7.3 仓库、装备与商人

```text
GET  /api/inventory
GET  /api/inventory/capacity
GET  /api/loadout
GET  /api/loadout/capacity
PUT  /api/loadout
GET  /api/merchants
GET  /api/merchants/:id/catalog
POST /api/merchant/purchase
POST /api/merchant/sell
POST /api/armor/repair
```

### 7.4 探索

```text
POST /api/session/start
GET  /api/session/:id
GET  /api/session/:id/events
GET  /api/session/:id/events/stream
GET  /api/sessions
POST /api/session/:id/abort
```

探索启动返回 `202 Accepted`。风险预估接口已移除；已完成、已中止和失败 Session 通过事件 REST 接口回放历史。

### 7.5 后续 API

```text
POST /api/v1/hideout/jobs
GET  /api/v1/hideout/jobs
POST /api/v1/hideout/jobs/:id/cancel
```

藏身处队列尚未实现，正式加入时应随 API 版本化一起设计。

## 8. 关键风险和控制措施

| 风险 | 影响 | 控制措施 |
|---|---|---|
| 用户作用域遗漏 | 数据越权 | Repository 强制 `user_id`，双用户集成测试 |
| Worker 重复领取 | 重复奖励 | 数据库 lease、单局唯一键、事件唯一键、Pending Run hash |
| 长时间离线补算 | 长事务、延迟 | 每次 Worker 最多补算 100 局，逐局短事务提交 |
| 更新数值破坏旧行动 | 无法重放 | 保存 `engine_version + scenario_snapshot + scenario_hash` |
| SSE 连接过多 | 连接资源耗尽 | 心跳、连接上限、轮询回退 |
| migration 失败 | 无法启动或数据损坏 | 预发演练、备份、独立 migration job |
| 经济逻辑并发错误 | 负库存或刷钱 | 事务、约束、幂等、流水审计 |
| 服务重启丢失行动推进 | 行动停止 | `next_run_at`、`last_processed_at`、lease 与 Pending Run |
| 静态定义覆盖玩家状态 | 存档损坏 | 静态定义与用户状态分表 |
| 登录接口被攻击 | 账号风险 | Argon2id、限流、失败退避、Session 撤销 |

## 9. 当前暂缓内容

在 R9 生产基础完成前，暂缓：

- 玩家间交易和拍卖行。
- 公会、聊天和多人协作。
- WebSocket 双向战斗操作。
- 动态全服市场。
- 跨服排行榜和赛季重置。
- 复杂武器配件和身体部位系统。
- 微服务、消息中间件和 Kubernetes。

这些功能都会显著扩大安全、并发和经济一致性范围，应该建立在稳定的多用户、事件和结算基础上。

## 10. 参考规范

- OWASP Password Storage Cheat Sheet：<https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html>
- OWASP Session Management Cheat Sheet：<https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html>
- OWASP CSRF Prevention Cheat Sheet：<https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html>
- PostgreSQL Transaction Isolation：<https://www.postgresql.org/docs/current/transaction-iso.html>
- PostgreSQL `SELECT ... FOR UPDATE SKIP LOCKED`：<https://www.postgresql.org/docs/current/sql-select.html>
- MDN Server-Sent Events：<https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events>
