# 搜打撤放置游戏在线方向改造方案

> 版本：V1.0  
> 状态：实施基线  
> 适用项目：`D:/idle`  
> 目标：将当前单机单存档原型改造成支持用户登录、在线查看、离线挂机、局外养成和服务器部署的多用户在线游戏。

## 1. 改造目标

### 1.1 产品目标

完成改造后，系统应支持：

1. 用户注册、登录、退出和会话续期。
2. 每个用户拥有独立角色、仓库、装备、商人关系和藏身处状态。
3. 玩家在线时可以实时查看探索进度和关键行动事件。
4. 玩家离线后探索继续推进，重新上线时可以补算并查看离线结果。
5. 服务重启、升级或短暂故障后，行动可以继续推进，不重复结算。
6. 购买、出售、维修、掉落、行动结算和丢装结算具备事务一致性。
7. 后续可以通过增加 Worker 实例扩展处理能力。

### 1.2 第一阶段边界

第一阶段继续保持以下约束：

- 单用户只拥有一个可出战角色。
- 单角色同一时间最多进行一个探索会话。
- 不做玩家交易、拍卖行、公会和多人战斗。
- 不做微服务拆分。
- PostgreSQL 是生产环境唯一权威数据源。
- Redis 仅在出现明确性能需求后引入，不保存权威游戏状态。
- SQLite 保留给本地快速开发和纯引擎单元测试。

### 1.3 当前可复用能力

当前项目已经具备以下基础，可以继续复用：

- Go、Gin、GORM 的 API 服务骨架。
- Vue 3、TypeScript、Element Plus 前端工作台。
- 角色、装备、仓库、商人和护甲实例模型。
- 起点、撤离点、节点连接和容器搜刮模型。
- 自动战斗、掉落、携行容量和多局行动逻辑。
- seed 驱动的快速预估入口。

当前主要阻塞点：

- `PlayerCharacterID`、`PlayerLoadoutID` 等固定主键只支持一个存档。
- 仓库、护甲、商人好感度和行动记录没有用户归属。
- 全局只允许一个进行中会话。
- 后台行动依赖进程内 goroutine，服务重启后无法恢复。
- 行动结果主要保存在文本报告中，缺少结构化事件和游标。
- SQLite、启动时 `AutoMigrate` 和本地 CORS 配置不适合生产环境。
- API 没有认证、授权、幂等键、分页和统一错误格式。

## 2. 核心设计原则

### 2.1 在线和离线共享结果约束，允许不同模拟粒度

在线探索与离线挂机共享 seed、掉落表、奖励上限、消耗规则、伤势和停止条件。在线模式可以展示较细的节点事件；离线模式使用轻量的宏观分段结算，不生成每分钟或每个敌人的详细战斗过程。这样可以保留经济一致性，同时避免低配服务器执行大量无意义的细节模拟。

### 2.2 服务端权威

客户端只提交玩家意图，例如地图、装备和中止操作。以下内容必须由服务端计算：

- 服务器时间。
- 随机 seed 和 RNG 游标。
- 路线、事件和战斗结果。
- 掉落、价格、结算和物品消耗。
- 伤势恢复、设施完成和冷却结束时间。

### 2.3 用户数据强隔离

所有运行态数据必须能追溯到 `user_id`。任何包含资源读取或修改的查询，都必须同时检查资源 ID 和当前登录用户。

### 2.4 数据库保存事实

数据库保存角色当前状态、行动游标和已发生事件。SSE、缓存和内存状态只负责加速展示，不能成为游戏状态的唯一来源。正式多实例部署使用 PostgreSQL；2c4g 单机档位允许使用 SQLite WAL，前提是采用单进程、短事务和单写入调度。

### 2.5 结算可重试

行动推进、购买、出售、维修和奖励领取必须支持安全重试。相同操作重复提交时，只允许产生一次状态变化。

### 2.6 分阶段替换

每一步先建立新能力，再迁移调用方，最后删除旧路径。数据库结构变更优先采用可向前兼容的新增字段和新增表，避免一次性重写全部模块。

## 3. 目标架构

```text
Browser
  |
  | HTTPS / SSE
  v
Caddy or Nginx
  |-- Vue static assets
  `-- /api/* -> Go API
                   |
                   |-- Authentication middleware
                   |-- Game command/query services
                   |-- Realtime event stream
                   |
                   v
              PostgreSQL
                   ^
                   |
              Session Worker
              Facility Worker
```

常规生产架构可以由一个 Go 进程同时运行 API 和 Worker。2c4g 单机档位不部署独立消息队列、Redis 或多个 Worker，先使用一个 API 进程、一个低频调度循环和一个轻量结算执行器。代码层仍保持 API、调度和结算生命周期独立，后续扩容时才能拆分。

推荐逐步形成以下目录边界：

```text
backend/
├── cmd/
│   ├── server/
│   └── worker/
├── internal/
│   ├── auth/
│   ├── account/
│   ├── game/
│   │   ├── content/
│   │   ├── engine/
│   │   ├── expedition/
│   │   └── progression/
│   ├── economy/
│   ├── repository/
│   ├── realtime/
│   └── transport/http/
└── migrations/
```

这只是目标边界。实施时从现有包逐步迁移，不要求第一步就完成目录重组。

## 4. 目标数据模型

### 4.1 账号与登录会话

| 表 | 关键字段 | 说明 |
|---|---|---|
| `users` | `id, username, email, password_hash, status, created_at` | 用户主表 |
| `auth_sessions` | `id, user_id, token_hash, expires_at, last_seen_at, revoked_at` | 服务端登录会话 |

约束：

- `username` 和规范化后的 `email` 唯一。
- 数据库只保存 Session Token 的哈希值。
- 用户禁用后立即拒绝新请求，并撤销现有登录会话。

### 4.2 玩家运行态

| 表 | 用户归属 | 改造方式 |
|---|---|---|
| `characters` | `user_id` | 每个用户第一阶段只创建一个角色 |
| `player_loadouts` | 通过 `character_id` | 删除固定主键假设 |
| `inventories` | `user_id` | 唯一索引包含 `user_id` |
| `armor_instances` | `user_id` | 防止读取或维修他人实例 |
| `user_merchant_states` | `user_id + merchant_id` | 将商人定义与玩家好感度拆开 |
| `hideout_facilities` | `user_id + facility_id` | 保存设施等级和运行状态 |
| `facility_jobs` | `user_id` | 保存制造、治疗和升级队列 |

静态内容继续使用共享定义表：

- 武器、护甲、补给和战利品定义。
- 地图、节点、容器、敌人和事件定义。
- 商人和藏身处设施定义。

### 4.3 探索会话

建议将当前 `sessions` 扩展为持久化行动实体：

| 字段 | 作用 |
|---|---|
| `id` | 对外使用 UUID/ULID，对内可保留数字主键 |
| `user_id` | 行动所有者 |
| `character_id` | 出战角色 |
| `status` | `scheduled/running/waiting/aborting/finished/failed` |
| `seed` | 随机种子 |
| `rng_cursor` | 已消费的随机序列位置或确定性步骤号 |
| `engine_version` | 模拟规则版本 |
| `content_version` | 地图、物品和数值版本 |
| `started_at` | 开始时间 |
| `finish_before` | 会话最晚结束时间 |
| `simulated_until` | 已推进到的游戏时间 |
| `next_event_at` | 下一次可推进时间 |
| `event_sequence` | 最后生成的事件序号 |
| `state_snapshot` | 当前局内状态 JSONB |
| `version` | 乐观锁版本 |

行动启动时必须保存完整快照：

- 角色属性和技能。
- 武器、护甲、胸挂、背包、头盔、耳机和补给。
- 护甲实例 ID 与当前耐久。
- 地图、行动策略和恢复预设。
- 引擎版本和内容版本。

### 4.4 结构化事件

新增 `expedition_events`：

```text
id
expedition_id
sequence
occurred_at
game_minute
event_type
payload JSONB
created_at
```

建议事件类型：

```text
run_started
node_entered
container_searched
loot_found
loot_discarded
encounter_started
battle_resolved
consumable_used
extraction_started
run_settled
injury_started
session_stopped
```

唯一约束：

```sql
UNIQUE (expedition_id, sequence)
```

前端行动报告由事件渲染生成。资源扣除直接读取事件结果对象，禁止通过报告文本匹配关键词。

### 4.5 经济流水

新增 `economy_ledger`：

```text
id
user_id
operation_id
operation_type
currency_delta
item_id
item_delta
reference_type
reference_id
created_at
```

`operation_id` 必须唯一，用于保证购买、出售、行动结算和奖励领取不会重复执行。

## 5. 详细实施顺序

## 5.1 第 0 步：冻结当前基线

### 目标

建立可重复验证的单机基线，防止服务化改造期间无法判断玩法是否发生回归。

### 后端工作项

1. 为当前数据库建立只读备份，不直接使用唯一的 `idle.db` 做迁移实验。
2. 固定一组角色、装备、地图和 seed 组合。
3. 将 `simulateSingleRun` 的关键结果固化为测试样例。
4. 补充以下测试：
   - 起点到撤离点的路线合法。
   - 容器搜刮受格数和重量限制。
   - 同 seed、同输入得到相同输出。
   - 弹药、护甲和补给不会出现负数。
   - 单局结算只执行一次。
5. 记录当前 1000 次预模拟的撤离率、伤势率、收益和消耗区间。

### 前端工作项

1. 保存探索、角色、仓库和日志页面的关键截图。
2. 记录当前 API 响应格式，作为后续 DTO 迁移对照。

### 验收条件

- `go test ./...` 稳定通过。
- 相同 seed 可以稳定复现。
- 已记录基线分布和页面状态。

### 回滚方式

本步骤不修改生产数据结构，只需要恢复代码和数据库备份。

## 5.2 第 1 步：建立配置和数据库迁移体系

### 目标

让项目可以在本地 SQLite 和生产 PostgreSQL 之间切换，并停止依赖生产启动时自动建表。

### 后端工作项

1. 增加配置结构：

```text
APP_ENV
HTTP_ADDR
DATABASE_DRIVER
DATABASE_URL
SESSION_SECRET
TRUSTED_PROXIES
ALLOWED_ORIGINS
```

2. 将数据库初始化从 `main.go` 移到独立 `repository/database` 包。
3. 支持 `sqlite` 和 `postgres` 两种驱动。
4. 建立版本化 SQL migration 目录。
5. 本地开发可以保留显式 migration 命令，生产启动禁止直接调用 `AutoMigrate`。
6. seed 分为两类：
   - 静态内容 seed，可以幂等更新。
   - 玩家初始数据，只在注册事务中创建一次。
7. 增加数据库连接池、连接超时和事务超时配置。

### 数据迁移

第一份 migration 只复刻当前结构，不立即增加用户字段。先验证 PostgreSQL 与现有查询行为一致。

### 验收条件

- 同一套集成测试可以分别运行在 SQLite 和 PostgreSQL。
- 空 PostgreSQL 数据库可以通过 migration 和静态 seed 启动。
- 重复执行 migration 不会破坏已有数据。
- 生产模式缺少必要配置时拒绝启动。

### 回滚方式

部署前保留旧 SQLite 服务。PostgreSQL 验证失败时切回旧版本和旧数据库，禁止双向写入。

## 5.3 第 2 步：引入用户归属和数据隔离

### 目标

先建立多用户数据边界，再接入登录入口。

### 数据库工作项

1. 创建 `users` 表。
2. 给 `characters`、`inventories`、`armor_instances`、`sessions` 增加 `user_id`。
3. 将商人表拆分：
   - `merchant_defs` 保存共享定义。
   - `user_merchant_states` 保存好感度、解锁和个人状态。
4. 给运行态表增加外键和组合索引。
5. 库存唯一索引调整为：

```sql
UNIQUE (user_id, item_id, raid_extract)
```

6. 限制单角色进行中行动：

```sql
CREATE UNIQUE INDEX uq_active_expedition_character
ON expeditions(character_id)
WHERE status IN ('scheduled', 'running', 'waiting', 'aborting');
```

### 数据迁移工作项

1. 创建一个迁移用默认用户。
2. 将现有角色、仓库、装备实例、会话和商人好感度归属到默认用户。
3. 所有 `user_id` 回填完成后再增加 `NOT NULL`。
4. 验证数量、现金、装备耐久和历史行动前后完全一致。

### 后端工作项

1. 删除 `PlayerCharacterID` 和 `PlayerLoadoutID` 业务依赖。
2. Service 方法显式接收 `userID` 或 `characterID`。
3. Repository 查询统一包含用户作用域。
4. `ListSessions`、`GetSession`、维修、购买、出售全部增加归属校验。
5. 全局“存在进行中行动”改成当前角色范围判断。

### 测试要求

创建用户 A 和用户 B，覆盖：

- A 无法读取、修改、中止或结算 B 的行动。
- A 无法维修或出售 B 的装备。
- 两个用户可以同时开始行动。
- 相同物品可以分别存在于两个用户仓库中。

### 验收条件

- 所有运行态表都有明确用户归属。
- 越权测试全部返回 `404` 或统一的无权限响应。
- 业务代码中不再引用固定玩家主键。

### 回滚方式

本阶段采用新增字段和新表。旧字段暂时保留一版，确认新查询稳定后再删除。

## 5.4 第 3 步：实现登录认证和授权中间件

### 目标

建立安全的网页登录会话，并让全部玩家 API 从登录上下文获得 `user_id`。

### 后端工作项

1. 实现接口：

```text
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/logout
GET  /api/v1/auth/me
```

2. 密码使用 Argon2id，并限制注册和登录请求频率。
3. 生成高熵随机 Session Token，数据库保存 `token_hash`。
4. Cookie 建议：

```text
__Host-session=<token>
Secure
HttpOnly
SameSite=Lax
Path=/
```

5. 登录成功后轮换 Session ID。
6. 登录 Session 保存过期时间、最后活跃时间、撤销时间。
7. 增加 `AuthMiddleware`，把 `user_id` 写入 Gin Context。
8. 所有玩家私有接口移入认证路由组。
9. 写接口增加 CSRF Token 或自定义 Header 校验，并校验 `Origin`。
10. 增加登录失败退避、账号禁用和 Session 撤销能力。

### 前端工作项

1. 引入 Vue Router，至少包含：

```text
/login
/register
/game
```

2. Axios 开启同源 Cookie，并统一处理 `401`。
3. 未登录时进入登录页，登录成功后请求 `/auth/me`。
4. 退出登录后清空内存中的玩家状态。
5. 禁止把 Session Token 保存到 `localStorage`。

### 验收条件

- 未登录请求玩家资源返回 `401`。
- 登录后只能访问当前用户数据。
- Cookie 无法被前端 JavaScript 读取。
- 退出登录后旧 Session 立即失效。
- 修改密码或禁用账号后可以撤销全部 Session。

### 回滚方式

保留只在测试环境启用的临时默认用户登录入口。生产环境禁止保留绕过认证的兼容路径。

## 5.5 第 4 步：稳定 API 合同和前端启动流程

### 目标

减少首屏请求数量，建立适合长期演进的 API 版本和错误规范。

### 后端工作项

1. 新接口统一使用 `/api/v1`。
2. 统一错误格式：

```json
{
  "code": "EXPEDITION_ALREADY_RUNNING",
  "message": "当前角色已有行动正在进行",
  "requestId": "..."
}
```

3. 增加 `/api/v1/bootstrap`，返回：
   - 当前用户和角色摘要。
   - 当前装备和仓库容量。
   - 当前行动摘要。
   - 静态内容版本。
4. 静态内容提供独立版本化接口，支持 `ETag` 或版本号缓存。
5. 历史行动和事件接口增加游标分页。
6. 为购买、出售、维修、开始行动等写接口接受 `Idempotency-Key`。
7. API DTO 与 GORM Model 分离，避免数据库字段直接成为长期接口合同。

### 前端工作项

1. 将当前首屏多个并发请求替换为 `bootstrap + static-content`。
2. 静态内容按 `contentVersion` 缓存。
3. 对 `401`、`403/404`、`409`、`429` 建立明确交互。
4. 当前行动状态独立管理，避免刷新仓库时覆盖实时行动数据。

### 验收条件

- 登录后的核心首屏请求减少到 2 至 4 个。
- 重复提交写请求不会产生重复状态变化。
- 历史行动数量增加后不会一次性返回全部记录。

### 回滚方式

`/api` 旧接口保留一个迁移版本，前端全部切到 `/api/v1` 后再删除。

## 5.6 第 5 步：拆出确定性模拟引擎

### 目标

让模拟逻辑脱离数据库、HTTP 和后台 goroutine，成为可以重放和批量验证的纯逻辑模块。

### 引擎接口

建议形成类似接口：

```go
type StepInput struct {
	Snapshot       ExpeditionSnapshot
	State          ExpeditionState
	Seed           int64
	Sequence       int64
	TargetGameTime int
}

type StepOutput struct {
	State     ExpeditionState
	Events    []DomainEvent
	Inventory []InventoryDelta
	Wallet    []WalletDelta
	NextAt    time.Time
}
```

### 后端工作项

1. 把地图寻路、容器搜刮、事件、战斗和撤离移入 `game/engine`。
2. 引擎输入只能使用完整快照，禁止在模拟中临时查询当前装备或当前商人状态。
3. 引擎输出结构化 `DomainEvent` 和资源 Delta。
4. RNG 由 seed 与 sequence 驱动，禁止直接使用当前时间生成中间随机数。
5. 保存 `engine_version` 和 `content_version`。
6. 老行动继续使用启动时版本，数值更新只影响新行动。
7. 报告渲染器根据结构化事件生成中文文本。
8. 删除通过 `strings.Contains(report, ...)` 判断资源消耗的路径。

### 测试要求

- 同输入、同版本、同 seed、同 sequence 输出完全相同。
- 引擎测试不连接数据库。
- 10000 次模拟中不存在非法路线、负资源或超容量携带。
- 每种装备和关键属性至少有一条效果测试。
- 预估接口与正式结算调用同一引擎。

### 验收条件

- 引擎可以在命令行测试中独立运行。
- seed 重放能够还原完整事件序列。
- Service 只负责读取快照、调用引擎和事务结算。

### 回滚方式

保留旧模拟入口作为测试对照，禁止新旧引擎同时写正式玩家数据。

## 5.7 第 6 步：实现持久化行动调度和离线补算

### 目标

替换进程内 `go runSession`，让行动可以跨服务重启。2c4g 档位采用“懒结算”优先：离线期间只保存行动快照和时间，不持续执行模拟；玩家上线、打开行动页或主动领取时再计算离线结果。

### 时间语义

实施前固定以下规则：

1. 一分钟游戏时间对应多少现实时间。
2. 玩家可选择或系统提供的挂机时长档位。
3. 最大离线补算时长。
4. 服务器停机期间是否累计行动时间。建议累计。
5. 资源耗尽、重伤、护甲损坏和背包满的停止条件。
6. 手动中止在当前节点结算后生效。

### 调度方式

常规多实例部署可以让多个 Worker 使用数据库行锁领取到期行动。2c4g 单机档位只保留一个低频调度器，用于推进少量在线行动和处理主动触发的离线结算，不为每个角色创建 goroutine、定时器或数据库轮询作业。

多实例模式的领取 SQL：

```sql
SELECT id
FROM expeditions
WHERE status IN ('scheduled', 'running', 'waiting', 'aborting')
  AND next_event_at <= NOW()
ORDER BY next_event_at, id
FOR UPDATE SKIP LOCKED
LIMIT 50;
```

### 单次推进事务

```text
BEGIN
  锁定 expedition
  校验 status、version、sequence
  读取行动快照和当前 state_snapshot
  调用纯模拟引擎推进有限步数
  写入 expedition_events
  应用 inventory/economy delta
  更新 state_snapshot、sequence、simulated_until、next_event_at
  更新 version
COMMIT
```

2c4g 懒结算的事务缩减为：

```text
BEGIN
  锁定 expedition
  读取行动快照、seed 和 last_settled_at
  用 FastIdleSimulator 计算离线时间段摘要
  应用一次 inventory/economy delta
  写入 1 条 summary event 和少量 notable event
  更新 status、last_settled_at、summary、version
COMMIT
```

### 关键约束

- 单次事务只推进有限事件，避免一次补算 24 小时导致长事务。
- 事件唯一键保证 Worker 崩溃重试时不重复写入。
- `version` 或行锁防止中止请求和 Worker 同时结算。
- Worker 关闭时停止领取新作业，允许当前事务完成。
- 启动时自动扫描到期行动，不依赖内存作业列表。
- 单个行动失败只标记该行动，不影响 Worker 主循环。

### 在线与离线行为

- 玩家在线：低频调度器每 15～30 秒推进一次轻量事件，前端使用 5 秒游标轮询查看状态。
- 玩家离线：不执行持续模拟，只记录服务器时间和行动快照。
- 玩家上线或打开行动页：执行一次懒结算，结算成功后把 `last_settled_at` 推进到当前时间。
- 24 小时离线按 30 分钟一个宏观分段，最多计算 48 段；超过上限的时间按挂机上限截断。
- 补算超过单次 CPU 预算时返回“补算中”，由单个低优先级作业继续处理，登录请求不能同步等待完整历史。

### 2c4g 标准：FastIdleSimulator

离线挂机不调用当前逐节点战斗循环，新增独立的轻量结算函数。它只保留影响经济和行动终态的因素：

1. 用 `seed + segment_index` 派生确定性随机数，重试不会改变结果。
2. 每个 30 分钟游戏时间段只做一次遭遇判定、一次搜刮抽样和一次装备耐久/弹药消耗计算。
3. 掉落按分类权重和数量上限聚合，不逐个生成容器、子弹和战斗日志。
4. 伤势、护甲损坏、弹药耗尽、容量不足和撤离条件触发时立即结束后续分段。
5. 只保存汇总字段，例如局数、成功局数、获得物品、消耗物资、伤势和终止原因。
6. 详细日志最多保留 10 条重要事件；普通分段不落明细报告。

在线模式可以继续使用细粒度模拟，但在线与离线必须共享掉落表、奖励封顶和状态变更函数，防止通过切换模式刷收益。

### 容量模型与并发边界

百个离线角色只应等价为百行待结算数据，不应等价为百个永久运行的完整 goroutine。2c4g 方案把离线计算从“持续负载”改为“领取时短暂负载”。

当前原型的负载放大方式如下：

```text
单次离线补算量 = 离线游戏时长 / 平均单局游戏时长 × 每局节点数
总补算量 = 同时补算角色数 × 单次离线补算量
```

例如 100 个角色同时补算 1440 分钟，平均 20 分钟一局、每局 4 个节点，当前实现大约需要处理 7200 局、28800 个节点。FastIdleSimulator 改成 48 段宏观计算后，最多只需处理 4800 个轻量分段，并且每个角色只执行一次结算事务。

2c4g 单机建议配置：

| 项目 | 建议值 |
|---|---|
| Go 进程 | 1 个 API + 调度器进程 |
| 调度并发 | 在线行动 1 个执行槽，离线懒结算 1 个低优先级执行槽 |
| 数据库 | 优先 SQLite WAL；需要多实例时再迁移外部 PostgreSQL/MySQL |
| SQLite 连接 | `MaxOpenConns=4`，设置 busy timeout，所有经济变化短事务提交 |
| 实时查看 | 首版使用 5 秒游标轮询，暂不启用 SSE 长连接 |
| 日志 | 汇总存储，单行动最多 10 条重要事件 |
| 单次离线时长 | 默认最多 24 小时，按 30 分钟分段 |
| 同时在线细模拟 | 首版控制在 20～30 个行动以内 |

在该配置下，数百个离线行动可以保持待结算状态，压力主要来自用户上线时的轻量结算。必须压测 100 个角色同时领取 24 小时收益、20～30 个在线行动持续查看、SQLite 写入冲突和服务重启恢复。建议门槛为：普通 API P95 小于 300 毫秒、单个离线结算 P95 小于 200 毫秒、数据库 CPU 长时间低于 70%、没有持续锁等待。若超过门槛，先限制同时结算数量和挂机时长，再考虑升级数据库或增加机器。

### 验收条件

- 行动过程中重启 API 和 Worker，恢复后继续生成正确事件。
- 两个 Worker 同时运行不会重复结算。
- 在线持续运行与离线懒结算共享经济约束，并在相同输入下得到可重放的合法终态；两者不要求事件明细粒度完全相同。
- 中止和结算并发时只出现一个合法终态。

### 回滚方式

上线前停止创建新行动，等待旧 goroutine 会话结束，再切换持久化调度。禁止在同一个行动上混用两套调度器。

## 5.8 第 7 步：实现实时行动查看

### 目标

让玩家在线时看到关键行动节点，并在断线重连后继续追赶。

### 第一阶段：游标轮询

接口：

```text
GET /api/v1/expeditions/:id/events?after=128&limit=100
```

响应包含：

```json
{
  "events": [],
  "nextCursor": 142,
  "caughtUp": true,
  "sessionStatus": "running"
}
```

当前 5 秒轮询可以继续使用，但必须从“重新读取完整会话”调整为“按事件游标增量读取”。

### 第二阶段：SSE

接口：

```text
GET /api/v1/expeditions/:id/stream
Last-Event-ID: 142
```

SSE 只负责通知和推送已持久化事件。断线后客户端使用最后事件 ID 补拉数据库记录。

### 前端状态机

```text
connecting
live
reconnecting
catching_up
finished
failed
```

### 展示粒度

推荐实时推送：

- 进入节点。
- 搜索容器和发现关键物资。
- 遭遇、战斗摘要和资源消耗。
- 开始撤离和最终结算。

普通命中和每发子弹保留在展开后的详细报告中，避免实时流产生过量事件。

### 验收条件

- 页面刷新后从最后游标继续，无重复日志。
- SSE 断开后能够自动补拉。
- 用户只能订阅自己的行动。
- 慢客户端不会阻塞 Worker 和其他用户。

### 回滚方式

SSE 出现问题时关闭实时流入口，前端回退到游标轮询，数据库事件模型不需要回滚。

## 5.9 第 8 步：强化经济、库存和幂等结算

### 目标

让所有局内外资源变化具备审计、并发安全和防重复能力。

### 后端工作项

1. 购买、出售、维修、补购、行动结算全部生成 `operation_id`。
2. 钱包与库存更新放在同一事务中。
3. 统一资源锁定顺序：

```text
user wallet
inventory stack
equipment instance
expedition
```

所有业务保持同一顺序，降低死锁概率。

4. 数据库增加约束：
   - 现金余额不得为负。
   - 库存数量不得为负。
   - 护甲当前耐久不能超过最大耐久。
5. 商人好感度、开放状态和购买解锁改为用户维度。
6. 当前装备和行动快照中的装备禁止被出售或重复使用。
7. 仓库容量检查和物品写入必须处于同一事务。
8. 记录所有管理补偿和异常修复流水。

### 测试要求

- 同一购买请求并发提交十次只扣一次钱。
- 行动结算超时重试不会重复发放战利品。
- 中止、丢装、补购和维修并发时库存保持合法。
- 用户无法出售行动中已锁定装备。

### 验收条件

- 任意资源变化都可以通过 `operation_id` 追溯来源。
- 数据库中不存在负现金、负库存和无归属装备。

### 回滚方式

流水表只追加不删除。业务回滚通过生成反向补偿操作完成，不直接修改历史流水。

## 5.10 第 9 步：实现局外养成和持久队列

### 目标

让藏身处、治疗、训练和制造使用与探索相同的持久化时间模型。

### 设施模型

共享定义：

```text
facility_defs
facility_level_defs
recipe_defs
```

玩家状态：

```text
hideout_facilities
facility_jobs
```

### 实施顺序

1. 医疗床：缩短或管理伤势恢复。
2. 维修台：维修作业进入队列，完成后修改实例耐久。
3. 储物间：设施等级决定仓库容量。
4. 工坊：消耗物资制造补给。
5. 情报室：提升预估信息质量和地图情报。
6. 训练场：消耗时间和资源获得定向熟练经验。

### 调度方式

`facility_jobs.complete_at` 使用与行动 Worker 相同的到期领取方式，不为每个作业创建独立 goroutine 或定时器。

### 验收条件

- 玩家离线时设施作业按服务器时间完成。
- 服务重启不会丢失制造、治疗或升级作业。
- 设施消耗和产出有完整流水。

### 回滚方式

设施能力按功能开关逐项启用。发生问题时暂停领取新作业，保留已创建作业并通过补偿事务处理。

## 5.11 第 10 步：生产部署基础设施

### 目标

建立可重复部署、可观察、可备份和可回滚的生产环境。

### 部署单元

建议准备：

```text
frontend build artifact
Go application image
PostgreSQL
Caddy or Nginx
backup job
```

初期部署可以使用 Docker Compose。2c4g 单机版的实际部署单元收敛为：

```text
Caddy/Nginx + Go API/调度器 + SQLite WAL + 备份作业
```

2c4g 档位暂不部署 Redis、Kafka、独立 Worker 和独立 PostgreSQL 容器，避免内存、连接数和运维开销被基础设施消耗。用户量和可用性要求提高后，再迁移到托管数据库并拆分 Worker。

2c4g 运行约束：

- Go 进程只启动一个实例，使用优雅关闭和单实例锁，避免 SQLite 多进程写入。
- SQLite 开启 WAL、busy timeout 和定期 checkpoint；数据库文件放在持久化磁盘。
- 数据库连接池限制为 4 个连接，所有库存、奖励和行动结算使用短事务。
- 日志按大小轮转，禁止无限保存逐节点报告。
- 每日备份 SQLite 文件，备份完成后执行一次恢复演练。

### 网络和 TLS

1. 浏览器只访问 HTTPS。
2. Vue 与 API 使用同一主域，减少 CORS 和 Cookie 复杂度。
3. 反向代理负责 TLS、请求体限制和基础安全 Header。
4. Go 配置真实可信代理范围，禁止无条件信任外部转发头。
5. 生产 CORS 只允许明确域名，凭证模式禁止通配来源。

### 启动流程

```text
检查配置
等待数据库可用
执行独立 migration job
启动 API
启动 Worker
执行 readiness 检查
切换流量
```

### 健康检查

```text
GET /health/live
GET /health/ready
```

- `live` 只证明进程存活。
- `ready` 检查数据库连接和必要静态内容版本。
- Worker 额外暴露最后成功领取和结算时间。

### 优雅关闭

1. 收到关闭信号后停止接受新请求。
2. Worker 停止领取新作业。
3. 等待当前事务完成或达到超时。
4. 关闭 SSE 连接，并让客户端进入重连状态。
5. 关闭数据库连接。

### 验收条件

- 新环境可以从空数据库自动部署到可登录状态。
- API 滚动重启期间已开始行动不会丢失。
- migration 失败时不会启动新版本 API。
- 可以在规定时间内回退应用镜像。

## 5.12 第 11 步：监控、备份和安全加固

### 结构化日志

每条关键日志包含：

```text
request_id
user_id
expedition_id
operation_id
worker_id
duration_ms
error_code
```

禁止记录：

- 明文密码。
- 完整 Session Token。
- CSRF Token。
- 数据库连接密码。

### 指标

至少监控：

- HTTP 请求量、延迟和错误率。
- 注册、登录成功和失败次数。
- 当前在线用户和 SSE 连接数。
- 进行中行动数量。
- 到期行动积压数和最大延迟。
- Worker 成功、失败和重试次数。
- 行动结算耗时。
- 数据库连接池、慢查询和锁等待。
- 经济操作失败和幂等冲突数量。

### 备份

1. 定期 PostgreSQL 备份。
2. 备份文件异地或跨磁盘保存。
3. 定期执行恢复演练。
4. 恢复后校验用户数、现金总量、库存数量、行动事件连续性。
5. 版本升级前执行额外备份。

### 安全

- 登录、注册、密码重置和预估接口限流。
- 输入长度、枚举值和数量范围统一校验。
- 设置 CSP、`X-Content-Type-Options` 和合理的 Referrer Policy。
- 管理后台使用独立权限模型，不允许通过普通用户 Session 访问。
- 定期更新依赖并运行漏洞扫描。
- 提供用户 Session 列表和主动撤销入口。

### 验收条件

- 可以从日志追踪一次行动的完整生命周期。
- Worker 积压和结算异常能够触发告警。
- 已完成至少一次从备份恢复到测试环境的演练。

## 5.13 第 12 步：测试、灰度和正式发布

### 测试层级

#### 单元测试

- 密码哈希和 Session 校验。
- 地图寻路、事件、战斗和搜刮。
- 时间推进和离线补算。
- 报告事件渲染。

#### 集成测试

- PostgreSQL migration。
- 用户隔离和权限校验。
- 行动领取、行锁、重试和幂等。
- 钱包、库存和装备事务。

#### 端到端测试

```text
注册
登录
配置装备
开始行动
在线查看事件
关闭页面
等待离线推进
重新登录
补拉事件
成功撤离
检查库存和行动结算
```

#### 故障测试

- Worker 在结算事务前后崩溃。
- API 在开始行动请求返回前崩溃。
- SSE 频繁断开。
- 数据库短暂不可用。
- migration 中途失败。

#### 压力测试

逐步验证：

- 100、500、1000 个同时进行的行动。
- 多 Worker 同时领取到期作业。
- 大量用户同时上线补拉离线事件。
- 行动历史达到数万条后的分页性能。

### 灰度发布

1. 内部测试用户。
2. 小规模邀请用户。
3. 只开放注册，不开放付费和玩家交易。
4. 观察至少一个完整挂机周期。
5. 确认经济、积压、错误率和恢复流程稳定后扩大用户量。

### 正式发布门槛

- 两个用户之间不存在数据越权。
- 相同 seed 可以重放。
- 在线推进与离线懒结算的资源、奖励和终态约束一致，差异只允许存在于日志粒度。
- 服务重启不会丢失或重复结算。
- 重复购买、出售和奖励请求只生效一次。
- Worker 积压有监控和恢复方案。
- PostgreSQL 备份已经验证可恢复。
- 生产 migration 和应用镜像都可以回退。

## 6. 推荐迭代拆分

| 迭代 | 范围 | 可交付结果 | 前置依赖 |
|---|---|---|---|
| R1 | 基线测试、配置、PostgreSQL、migration | 可在 PostgreSQL 运行的单用户版本 | 无 |
| R2 | `user_id`、数据迁移、用户隔离 | 多个测试用户拥有独立存档 | R1 |
| R3 | 注册登录、Session Cookie、API v1 | 可登录的多用户版本 | R2 |
| R4 | 纯模拟引擎、结构化事件、seed 重放 | 可重放的行动结算 | R1 |
| R5 | 持久化行动、FastIdleSimulator、懒结算 | 2c4g 单机可在重启后继续行动，离线领取时低成本结算 | R3、R4 |
| R6 | 游标轮询、SSE、前端实时状态 | 在线实时查看与断线追赶 | R5 |
| R7 | 经济流水、幂等和装备锁定 | 可审计且防重复的经济系统 | R5 |
| R8 | 藏身处队列、治疗和制造 | 完整局外养成闭环 | R7 |
| R9 | 容器部署、监控、备份、灰度 | 可公开部署的生产版本 | R3-R8 |

R2 和 R4 可以在 R1 完成后并行开发，但合并时必须以 R2 的用户隔离模型作为最终数据库结构。

## 7. API 初步清单

### 7.1 认证

```text
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/logout
GET  /api/v1/auth/me
GET  /api/v1/auth/sessions
DELETE /api/v1/auth/sessions/:id
```

### 7.2 初始化和静态内容

```text
GET /api/v1/bootstrap
GET /api/v1/content/:version
```

### 7.3 玩家状态

```text
GET /api/v1/character
PUT /api/v1/character
GET /api/v1/loadout
PUT /api/v1/loadout
GET /api/v1/inventory
GET /api/v1/merchants
GET /api/v1/hideout
```

### 7.4 探索

```text
POST /api/v1/expeditions/preview
POST /api/v1/expeditions
GET  /api/v1/expeditions/current
GET  /api/v1/expeditions
GET  /api/v1/expeditions/:id
POST /api/v1/expeditions/:id/abort
GET  /api/v1/expeditions/:id/events
GET  /api/v1/expeditions/:id/stream
```

### 7.5 经济和局外养成

```text
POST /api/v1/merchant/purchases
POST /api/v1/merchant/sales
POST /api/v1/equipment/:id/repair
POST /api/v1/hideout/jobs
GET  /api/v1/hideout/jobs
POST /api/v1/hideout/jobs/:id/cancel
```

## 8. 关键风险和控制措施

| 风险 | 影响 | 控制措施 |
|---|---|---|
| 用户作用域遗漏 | 数据越权 | Repository 强制 `user_id`，双用户集成测试 |
| Worker 重复领取 | 重复奖励 | 行锁、事件唯一键、`operation_id` |
| 长时间离线补算 | 长事务、延迟 | 限制单批事件数，分批提交 |
| 更新数值破坏旧行动 | 无法重放 | 锁定 `engine_version/content_version` |
| SSE 连接过多 | 连接资源耗尽 | 心跳、连接上限、轮询回退 |
| migration 失败 | 无法启动或数据损坏 | 预发演练、备份、独立 migration job |
| 经济逻辑并发错误 | 负库存或刷钱 | 事务、约束、幂等、流水审计 |
| 服务重启丢失行动推进 | 行动停止 | 数据库游标和 `next_event_at` |
| 静态定义覆盖玩家状态 | 存档损坏 | 静态定义与用户状态分表 |
| 登录接口被攻击 | 账号风险 | Argon2id、限流、失败退避、Session 撤销 |

## 9. 当前暂缓内容

在 R9 生产基础完成前，暂缓：

- 玩家间交易和拍卖行。
- 公会、聊天和多人协作。
- WebSocket 双向战斗操作。
- 动态全服市场。
- 跨服排行榜和赛季重置。
- 复杂武器配件和身体部位系统。
- 微服务、消息中间件和 Kubernetes。

这些功能都会显著扩大安全、并发和经济一致性范围，应该建立在稳定的多用户、事件和结算基础上。

## 10. 参考规范

- OWASP Password Storage Cheat Sheet：<https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html>
- OWASP Session Management Cheat Sheet：<https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html>
- OWASP CSRF Prevention Cheat Sheet：<https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html>
- PostgreSQL Transaction Isolation：<https://www.postgresql.org/docs/current/transaction-iso.html>
- PostgreSQL `SELECT ... FOR UPDATE SKIP LOCKED`：<https://www.postgresql.org/docs/current/sql-select.html>
- MDN Server-Sent Events：<https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events>
